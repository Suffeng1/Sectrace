# Handoff: H-01

- 角色：00 主控与运行时集成
- Ticket：H-01 AgentTeams discovery and smoke proof
- 状态：实现完成，等待 QA；运行时清理缺陷转为非阻塞 `H-01-RUNTIME-CLEANUP`
- 修改文件：`docs/runtime/hiclaw-compatibility.md`、`docs/runtime/hiclaw-smoke-evidence.md`、`docs/runtime/H-01-RUNTIME-CLEANUP.md`、`docs/adr/ADR-001-agentteams-runtime.md`、`docs/status.md`、`hiclaw/sectrace-agents/smoke-worker.yaml`、`hiclaw/sectrace-agents/smoke-team.yaml`、`tests/runtime/`、本 Handoff
- 权威 schema：安装包 CRD `/opt/agentteams/config/crd/workers.agentteams.io.yaml` 与 `/opt/agentteams/config/crd/teams.agentteams.io.yaml`
- 已证明资源格式：`agentteams.io/v1beta1`；`Worker.spec.model/runtime/state`；`Team.spec.workerMembers` 且恰好一个 `team_leader`
- MCP 附件格式：`Worker.spec.mcpServers[]`，必填 `name`、完整 `url`，可选 `transport=http|sse`
- 生命周期命令：`agt apply -f`、`agt get workers|teams <name> -o json`、`agt delete team|worker <name>`；本安装版不支持 `agt apply -f -`
- RED 摘要：首次运行 `tests/runtime/test_hiclaw_resource_files.py`，因两个 smoke YAML 缺失而 3/3 按预期失败
- GREEN 摘要：运行时与仓库卫生测试 9/9 通过
- 创建证据：`sectrace-smoke` 达到 `Running`，模型 `qwen3.6-plus`，运行时 `openclaw`，Matrix 用户/房间存在
- Team 证据：`sectrace-smoke-team` 从 `Pending` 收敛为 `Active`，Leader Ready，Team Room 与 Leader DM Room 存在
- 清理证据：官方 Team 删除报告成功但资源仍存在；Worker 删除因 Team 成员关系返回 HTTP 409；Controller 单容器重启后复现
- 风险处理：残留与复现已登记为 `docs/runtime/H-01-RUNTIME-CLEANUP.md`，不阻塞 T-01 或代码票据
- 安全边界：未读取 `.env`、token、password、credential、secret、容器资源 BLOB 或 `hiclaw/start_hiclaw.py`；未创建四个正式 Worker；未执行 T-01
- QA 重点：资源 YAML 是否严格匹配安装 CRD、无敏感字段、证据是否支持创建/Manager/Matrix 可见性、后置清理风险是否清晰且未被误报为已清理
- 是否请求主控修改 Contract：否
