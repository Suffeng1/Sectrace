# Handoff: R-06

- 角色：00 主控与运行时集成
- Ticket：R-06 minimal Worker runtime tool-policy repair
- 状态：**NEEDS-AUTHORIZATION**
- 日期：2026-08-06

## 已完成的只读诊断

- Commander 容器内 `mcporter` 二进制存在；`mcporter --help` 和 `mcporter call --help` 均以退出码 0 完成。
- `mcporter list` 和针对已注册 server 的列表命令均以退出码 0 完成。基于用户指定的安全输出限制，本次没有记录 URL、请求/响应正文、凭据、房间或其它运行时标识。
- Controller 的公开 Worker 查询可解析，但未暴露可审计的最终 system-prompt 来源、优先级或工具注入执行策略；因此无法把资源 prompt 文本等同于模型 turn 的实际执行权限。
- 既有脱敏证据仍支持：MCP initialize 成功、server 已登记、Commander 出现会话但未触发下游角色。D-06 结论保持 `FAIL`，根因优先级仍是“prompt-only 规则缺少运行时失败即停门禁”。

## 已执行的最小修复

- 已从当前分支 `HEAD` 精确恢复四份 Worker YAML，未恢复或修改其它资源文件。
- 四份 `spec.agents` 均新增失败即停门禁：只能经 mcporter CLI 调用；只有退出码 0、结构化安全 envelope 与当前 trace 结果齐备时才可生成下游 handoff；失败仅报告 `stage_failed` 与脱敏 `failure_class` 并停止。
- 四个 Worker 已按 Commander、Evidence、Response、Audit 顺序重新 apply，并逐个回到 Ready/Running。
- 未修改 MCP endpoint/server、Team YAML、凭据或模型；未重启 Manager/Controller，未操作 smoke 资源。

## 验证结果

- 已执行用户授权的唯一一次非 S01 合成 mcporter intake 调用。调用退出码为 1；只记录了失败类别，未记录请求/响应正文、URL、凭据、房间或标识。
- 该合成调用未重试；未发送 S01，未执行人工审批或真实处置。
- 仓库侧回归：`tests/runtime/test_production_agent_resources.py`、`tests/security/test_mcp_binding.py`、`tests/integration/test_mcp_adapter.py` 共 9 passed。

## 当前处置

- 静态策略与 Worker apply 成功，但实际 mcporter 调用仍失败；R-06 不能标记为运行时通过。
- 继续定位该失败需要新的精确授权，并且不得重复本次非 S01 合成调用；应仅先检查脱敏错误类别、CLI 参数契约和 server 侧调用策略。

## MCP listener 恢复结果

- 主机端口未监听且 MCP server 进程不存在；已按授权仅启动本地 MCP listener，并确认其仅监听 loopback。
- Commander 容器到 MCP 的无状态探测恢复为预期的 `406`；mcporter schema discovery 不再离线，五个安全工具与 intake 的 `scenario_id` 参数均可见。
- 本轮授权的唯一一次非 S01 合成 intake 调用退出码为 0，但未返回预期的安全 envelope，且输出包含脱敏失败类别；该结果不计为通过。
- 未记录调用正文，未重试，未发送 S01，未执行审批或真实处置。
- 传输恢复成功，但工具调用响应语义仍需新的精确授权后再诊断。

## MCP ToolResult 解析规则

- 四份 Worker prompt 已新增 MCP ToolResult 解包规则，并已逐个重新 apply 至 Ready/Running。
- 本轮授权的非 S01 格式验证确认 stdout 为含 `content` 和 `isError` 的 MCP ToolResult；`isError=true`，因此内层文本不是可接受的安全 envelope。
- 解析门禁按预期拒绝该结果：未生成 handoff、未重试、未发送 S01。
- 仓库侧资源/MCP 回归测试共 9 passed。
- 修复后的 prompt 能正确失败即停，但 intake 工具仍产生 MCP-level error；后续诊断需要新的精确授权。

## 显式 mcporter 调用示例

- schema 公开完整工具名，而未限定的 `<server.tool>` 示例会触发 `unsupported_tool`；四份 prompt 已改为显式 `--server` 与 `--tool` 形式，并已逐个重新 apply 至 Ready/Running。
- 本轮授权的唯一非 S01 合成验证仍返回 MCP ToolResult `isError=true`，没有可接受的内层 envelope；未重试、未发送 S01。
- 资源/MCP 回归结果为 1 failed、8 passed：`test_workers_require_mcporter_instead_of_direct_mcp_http` 仍硬编码旧调用示例。当前授权禁止修改测试，因此未同步该断言。
- 运行时 MCP-level error 与测试契约漂移均未解决，需要新的精确授权后继续。

## R-06 final minimal repair

- 根因已定位为 `KeyError: severity_hint`：合成 S05 的 `expected` 缺少该字段，而 Commander 构建事件时读取此必填严重性字段。
- `normalize_scenario` 现仅对非真实数据、且缺少该字段的场景注入 `low`；已有严重性值不变，真实数据拒绝规则不变。
- 已新增该缺省归一化单测与 S05 Commander 集成测试；运行时资源测试的七个 mcporter 示例均为显式 `--server sectrace --tool <完整工具名>` 格式，原有禁止直接 HTTP 的断言未变。
- 定向回归为 `8 passed`；完整仓库回归使用项目内 pytest 基准临时目录后为 `38 passed`。初次完整运行的 3 个错误仅为系统 pytest 临时目录权限问题，不是测试失败。
- 四个 Worker 已按 Commander、Evidence、Response、Audit 顺序重新 apply，均返回 `configured`。未操作 Manager、Controller、smoke 资源、MCP endpoint/server 注册、Team、凭据或模型。
- 未发送 S01、未执行人工审批或真实处置。

## Required authorization

状态：**NEEDS-AUTHORIZATION**。

最小修复已落地，但宿主 MCP listener 仍运行旧 Python 代码。完成最终非 S01 合成调用前，需要明确授权仅重启该本地 listener，然后执行一次使用合成 S05 的 mcporter 调用，验收退出码 0 与安全 envelope 解析；不得发送 S01、不得审批或真实处置。

## Listener restart and S05 validation

- 已按授权终止旧的 19090 listener，并用当前项目 Python 代码启动 replacement listener；新 listener 已确认监听该端口。
- 已执行唯一一次非 S01 S05 合成 mcporter 调用，未重试，未发送 S01，未执行审批或真实处置。
- 脱敏结果：退出码 `0`，外层 MCP ToolResult 可解析，`isError=false`。
- 验收未完全通过：调用响应中未检测到可解析的文本 content 项，因而不能解析或验证内层安全 envelope。只记录失败类别 `missing_text_content`，未记录正文或任何运行时标识。
- 后续如需继续，应单独授权只读检查成功 ToolResult 的 content 项结构及最小 prompt 解析修正；不得重复 S05 调用，除非另有明确授权。

## Direct-envelope parser repair

- 四份 Worker YAML 已将 stdout 解析改为双路径：顶层出现 `isError` 或 `content` 时按 ToolResult 解包；否则将顶层 JSON 直接作为安全 envelope。
- 两条路径均保留非零退出码与解析/字段/类型失败即 `stage_failed`、不生成下游 handoff 的门禁。
- 已按 Commander、Evidence、Response、Audit 顺序重新 apply，均返回 `configured`；运行时资源测试为 `4 passed`。
- 本轮唯一 S05 合成验证：退出码 `0`，采用 `direct_envelope` 路径；`schema_version`、`trace_id`、`safety_notice` 均为字符串，`result` 为对象，字段齐全。
- 因此已部署 prompt 的 handoff gate 条件成立。未实际生成或发送下游 handoff，未发送 S01，未执行审批或真实处置。
