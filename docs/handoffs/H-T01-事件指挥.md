# Handoff: T-01

- 角色：01 SecTrace 事件指挥
- Ticket：T-01 Commander / intake
- 状态：实现完成，等待 05 QA
- 修改文件：`src/agents/commander/service.py`、`src/skills/intake/README.md`、`src/skills/intake/normalize.py`、`tests/commander/test_service.py`、`tests/commander/test_normalize.py`、本 Handoff、`docs/status.md` 的 T-01 状态
- RED 1：`test_build_incident_from_s01` 因 Commander 服务缺失而按预期收集失败
- GREEN 1：S01 生成 schema v1.0 的 open `IncidentCase`，保留三个事件引用，并严格生成 `collect_evidence`、`plan_response`、`audit` 三项任务
- RED 2：`test_rejects_real_data_marker` 因 intake 规范化入口缺失而按预期收集失败
- GREEN 2：`real_data: true` 被明确拒绝；正常 S01 路径仍通过同一规范化入口
- 安全边界：仅接受合成或去标识数据；未生成调查结论，未连接、扫描或处置任何系统
- 共享资产：未修改 Contract、ADR 或 Git；未处理 `H-01-RUNTIME-CLEANUP`
- QA 重点：复验 S01 输出、单一非空 `trace_id`、事件引用顺序、严格任务列表，以及真实数据标记拒绝行为
- 是否请求主控修改 Contract：否
