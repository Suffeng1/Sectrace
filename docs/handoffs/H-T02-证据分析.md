# Handoff: T-02

- 角色：02 SecTrace 证据分析
- Ticket：T-02 Evidence
- 状态：实现完成，等待 05 QA
- 修改文件：`src/agents/evidence/service.py`、`src/skills/evidence/correlate.py`、`src/skills/evidence/README.md`、`tests/evidence/test_service.py`、`tests/evidence/test_insufficient.py`、本 Handoff、`docs/status.md` 的 T-02 状态
- RED 1：`test_s01_has_sourced_fact_and_risk_path` 因 Evidence 服务缺失而按预期收集失败
- GREEN 1：S01 输出三个有来源的 `fact`，按 supplied event 顺序形成 privilege-to-data-access 风险路径，并逐项保留 Commander `trace_id`
- RED 2：隔离 Commander 前置条件后，`test_incomplete_case_returns_unknown` 因 S05 被错误标记为 `fact` 而按预期失败
- GREEN 2：S05 输出 `unknown`、`insufficient`、低置信度和“无法确认”，且没有虚构风险路径
- RED 3：`test_rejects_real_data_marker` 因 Evidence 未拒绝 `real_data: true` 而按预期失败
- GREEN 3：Evidence 入口明确拒绝真实数据标记
- agent-eval：以 S01、S05 和真实数据拒绝三项固定合成任务作为确定性 pytest judge；Evidence 套件 3/3 PASS
- 安全边界：只处理 supplied synthetic/de-identified input；无外部日志连接、扫描、IOC 伪造或处置能力
- 共享资产：未修改 Contract、ADR 或 Git；未处理 `H-01-RUNTIME-CLEANUP`
- QA 重点：复验 `trace_id` 连续性、S01 来源与路径、S05 三项不足证据标记，以及真实数据标记拒绝
- 是否请求主控修改 Contract：否
- QA 路由：05 thread `019fcc0a-8edb-7c53-8e43-6220ae7e6c3e`

## Superseded status

原“等待 05 QA”是本 Handoff 形成时的尾状态。独立
`docs/verification/V-T02.md` 后续已判定 `PASS`；本节只更新当前状态链，不改写
上述 RED/GREEN 历史。
