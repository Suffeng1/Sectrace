# Handoff: T-05

- 角色：00 主控与运行时集成
- Ticket：T-05 MCP + four-Agent integration + local demo
- 状态：实现完成，等待 05 QA
- 核心链：S01 Commander → Evidence → Response → Audit，共用 `tr_s01`，默认审批为 pending，高风险计划始终 pending_approval
- MCP（本票 point-in-time）：`SafeMCPAdapter` 与 Streamable HTTP entry 当时仅暴露五个 allowlisted 工具；所有响应使用 v1.0 安全 envelope；未知/执行类工具拒绝。后续审批门加固加入 `sectrace.ledger.log_approval`，当前权威 `TOOL_NAMES` 为六工具。
- Ledger：内存 append-only canonical JSON + SHA-256 forward chain；Audit 验证 terminal hash
- UI：本地 Starlette 薄应用，`POST /api/demo/S01` 重放，页面明确“待人工审批”与无真实动作
- FastAPI 环境说明：项目声明 FastAPI，但本机离线代理无法安装；实现采用已安装 Starlette（FastAPI 的底层 ASGI 层）保持相同薄路由边界，无业务逻辑进入 route
- AgentTeams：四份 production Worker prompt/YAML 与一份 Team YAML 使用 H-01 已证明的 `agentteams.io/v1beta1`、`mcpServers` 和成员 schema；未 apply、未部署
- RED 1：orchestrator、MCP adapter、FastAPI/UI 缺失导致 3 个测试收集失败
- GREEN 1：T-05 integration/MCP/UI 测试 6/6 PASS（一个上游 TestClient deprecation warning）
- RED 2：四 Worker/Team 文件缺失导致 production-resource 测试 3/3 失败
- GREEN 2：资源 schema、prompt parity、Team 顺序与无凭据测试 3/3 PASS
- 安全：只使用合成 S01；无网络扫描、账号/权限/数据变更、shell 动作工具或真实处置；未读取凭据与 `hiclaw/start_hiclaw.py`
- 后置债务：未处理 `H-01-RUNTIME-CLEANUP`
- QA 重点（本票 point-in-time）：全套 pytest、当时五 MCP 工具及拒绝边界、S01 一 trace、pending/approved 两条本地审批投影、ledger tamper tests、UI 文案、YAML/prompt 安全、未部署事实
- 复现：`outputs/demo/demo-script.md`；证据索引：`outputs/demo/evidence-index.md`

## Superseded status

原“等待 05 QA”和五工具描述均是本 Handoff 形成时的事实。独立
`docs/verification/V-T05.md` 保留当时的 `FAIL`；后续六工具、live 与安全门由
`docs/verification/V-05-final-reconciliation.md` 判定 `PASS`。历史 FAIL 和
当时测试数字均未改写。
