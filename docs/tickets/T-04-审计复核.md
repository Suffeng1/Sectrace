# T-04: 审计复核

- **Owner**: 04 审计复核
- **Owned files**: `src/agents/audit/service.py`, `src/skills/audit/README.md`, `src/skills/audit/verify.py`, `tests/audit/test_service.py`, `tests/audit/test_redaction.py`, `tests/audit/test_integrity.py`, `docs/handoffs/H-T04-审计复核.md`
- **Input models**: `IncidentCase`, `list[EvidenceItem]`, `ResponsePlan`, `ApprovalRecord`, and validated ledger records.
- **Output model**: `AuditBundle` v1.0.
- **Red/green tests**: first run `test_high_risk_without_evidence_or_approval_is_not_qualified` red; implement deterministic projection; add/pass redaction and tampered-hash tests; run `pytest tests/audit -v` green.
- **Safety boundary**: do not invent evidence or approval; redact secret values; surface tampered ledger and missing requirements.
- **Completion condition**: audit preserves `trace_id`, flags missing items, omits secret values, and visibly fails integrity on tampering.
- **Handoff filename**: `docs/handoffs/H-T04-审计复核.md`
- **QA verification filename**: `docs/verification/V-T04.md`
