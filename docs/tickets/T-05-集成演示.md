# T-05: 集成演示

- **Owner**: 00 主控与集成
- **Owned files**: `src/app/ledger.py`, `src/app/orchestrator.py`, `src/app/main.py`, `src/app/agentteams_adapter.py`, `src/ui/`, `tests/integration/test_s01_flow.py`, `tests/e2e/test_demo_flow.py`, `README.md`, `requirements.md`, `docs/status.md`, `outputs/demo/demo-script.md`, `outputs/demo/evidence-index.md`
- **Input models**: S01 synthetic events, `IncidentCase`, `list[EvidenceItem]`, `ResponsePlan`, `ApprovalRecord`, ledger records.
- **Output model**: `AuditBundle` and one replayable demo trace.
- **Red/green tests**: first run `test_s01_flow_keeps_one_trace_id` red; implement only deterministic local orchestration; add/pass browser assertion for `待人工审批`; run `pytest -v` and documented Playwright command green.
- **Safety boundary**: local adapter performs no security action and cannot replace production acceptance evidence; high-risk plans stay pending human approval.
- **Completion condition**: a judge can run the documented local replay, select S01, see four role outputs, one `trace_id`, a pending human approval, and audit integrity state.
- **Required AgentTeams acceptance evidence**: real local HiClaw Manager, the four named Workers `sectrace-commander`, `sectrace-evidence`, `sectrace-response`, `sectrace-audit`, an S01 Matrix-visible task/context record, and a human approval interaction linked to that trace. Any missing item is V-05 `FAIL`.
- **Handoff filename**: `docs/handoffs/H-T05-集成演示.md`
- **QA verification filename**: `docs/verification/V-T05.md`
