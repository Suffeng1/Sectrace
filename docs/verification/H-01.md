# H-01 Independent Verification

- Result: **PASS**
- Verified commit: `fdf4d78` (`docs: verify local AgentTeams resource workflow`)
- Date: 2026-08-04
- QA scope: documentation, installed-schema contract, smoke resource definitions, redacted runtime evidence, safety boundaries, and repository tests

## Evidence reviewed

- The compatibility record identifies the installed Worker and Team CRDs as the authoritative source and records `agentteams.io/v1beta1` with the required Worker/Team fields.
- `smoke-worker.yaml` defines only `sectrace-smoke`, model `qwen3.6-plus`, runtime `openclaw`, and state `Running`.
- `smoke-team.yaml` defines only `sectrace-smoke-team` with one `team_leader` member, `sectrace-smoke`.
- Redacted smoke evidence records the Worker reaching `Running`, with Matrix user and room present.
- Redacted smoke evidence records the one-member Team reaching `Active`, with leader ready, Team Room present, and Leader DM Room present.
- The Handoff, ADR, compatibility record, smoke evidence, cleanup record, and resource definitions are mutually consistent.
- The repository hygiene test found no tracked credential-like value. No secret-bearing local runtime file was inspected.

## Independent test execution

Command scope: `tests/runtime` plus `tests/security/test_repository_hygiene.py`.

```text
.........                                                                [100%]
9 passed in 0.16s
```

The first sandbox run produced `8 passed, 1 failed` solely because the hygiene test's internal `git ls-files` encountered Git's dubious-ownership protection. Re-running with a process-local `safe.directory` value, without modifying global Git configuration, produced the result above.

## Runtime cleanup disposition

The supported Team delete command reported success but the Team remained visible; Worker deletion then returned HTTP 409 because membership remained. The same state persisted after a Controller-only restart. The residual resources are explicitly and accurately recorded as only:

- `sectrace-smoke-team`
- `sectrace-smoke`

They are not represented as cleaned up or as production Workers. Per the controller's accepted priority, this is tracked as the non-blocking environment debt `H-01-RUNTIME-CLEANUP` and is outside this verification's required force-cleanup scope.

## Conclusion

H-01 passes its schema discovery, smoke creation, Manager/Matrix visibility, resource safety, and test gates. T-01 may be unblocked. The runtime cleanup debt remains open and must not be mistaken for completed deletion.
