# V-T05 Full Verification

- Result: **FAIL**
- Ticket: T-05 MCP + multi-Agent integration + local demo
- Verified commit: `9048681` (`feat: add safe multi-agent demo integration`)
- Date: 2026-08-04
- Handoff: `docs/handoffs/H-T05-集成演示.md`

## Passing repository-local evidence

- Full independent repository test run passed:

```text
.................................                                        [100%]
33 passed in 1.32s
```

- An independent demo probe confirmed one unchanged `tr_s01` across Incident, Evidence, Response, Approval, Audit, and all ledger records.
- The deterministic stage order is Commander → Evidence → Response → Audit.
- Default replay remains `pending_approval` with approval `pending` and audit integrity `passed`.
- The local approved projection records approval while the response plan remains `pending_approval`; no plan becomes `executed`.
- Ledger records use canonical JSON and a SHA-256 forward hash chain; existing tamper tests pass.
- The actual FastMCP tool manager lists exactly five tools, matching the documented allowlist. Unknown and execution-style tool names are rejected.
- A short-lived independent startup probe confirmed the MCP process opens port 19090 and was then terminated.
- The Starlette UI/API e2e test passes and clearly shows the pending human gate.
- Four Worker YAML files, four matching prompts, and one Team YAML match the H-01-recorded `agentteams.io/v1beta1` schema and pass the repository credential-pattern checks.

Independent local probe summary:

```text
demo_and_mcp_probe=PASS
tool_count=5
mcp_loopback_port_ready=True
```

## Failing V-05 release gates

### 1. Production AgentTeams resources are not deployed

Exact-name, read-only queries against both `agentteams-controller` and `agentteams-manager` found none of the required live resources:

```text
sectrace-commander: absent
sectrace-evidence: absent
sectrace-response: absent
sectrace-audit: absent
sectrace-audit-team: absent
```

The committed YAML files are reproduction definitions only. They do not prove live Manager membership, Worker readiness, or runtime MCP attachment.

### 2. No live Matrix four-role collaboration evidence

There is no committed evidence of an S01 Manager task traversing the four production Workers through Matrix JSON handoffs with one trace ID. There are no redacted screenshots or message references for the required production Worker rooms/team flow. H-01 smoke-resource evidence cannot substitute for the production four-role V-05 run.

### 3. No live human intervention record

The local `approval_status="approved"` projection is a deterministic function argument, not a human action performed through Manager/Matrix. It proves approval-state behavior but does not satisfy the required live human approve/reject intervention evidence.

### 4. Missing final runtime-governance/security evidence

- No Higress-console governance evidence for the production run is present.
- No Codex Security release record establishes that there are no untriaged high-severity findings. The passing repository-hygiene test is useful but is not the required Codex Security review record.

## FastAPI / Starlette assessment

The Starlette implementation is acceptable as a small, tested local ASGI UI/API boundary under the updated repository T-05 requirements. Routes contain no business logic and the required replay behavior works. However, it is a disclosed deviation from the original V2 topology and tech-stack wording that specified FastAPI. It does not by itself cause this FAIL; the missing live AgentTeams/Matrix/human evidence does.

The MCP entry currently binds to `0.0.0.0` so containers can reach it via `host.docker.internal`; the demo UI command binds to `127.0.0.1`. The wider MCP bind should remain synthetic/read-only and should be restricted by host firewall or explicit interface policy for any non-isolated demonstration environment.

## Boundary disposition

- The operator's no-deployment instruction was respected. QA did not apply or delete Worker/Team resources, read credentials, inspect the local startup script, or attempt H-01 force-cleanup.
- `H-01-RUNTIME-CLEANUP` remains separate non-blocking environment debt and is not reopened by this result.
- Because live deployment was intentionally not authorized, passing repository-local evidence cannot be promoted into a V-05 PASS.

## Conclusion

The repository-local T-05 implementation is reproducible and its 33 tests pass, but V-05 is a release/demo gate that explicitly requires live Manager, four production Workers, Matrix-visible handoffs, and a live human intervention record. Those gates are objectively unmet. V-T05 is therefore **FAIL** until an authorized live run supplies the missing evidence and the final runtime/security records.
